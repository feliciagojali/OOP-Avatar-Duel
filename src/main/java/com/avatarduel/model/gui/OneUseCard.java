package com.avatarduel.model.gui;

/**
 * OneUseCard is an interface that used for one use cards in the controller
 * to differ between their uses (e.g : LandCard)
 * @author mkamadeus
 */
public interface OneUseCard {
    public void useCard();
}