package com.avatarduel.model.gui;

/**
 * Interface used to define a card that is used on the field
 * (either explicitly like character cards or implicitly like skill card with the destroy effect)
 * @author mkamadeus
 */
public interface FieldCard
{
    public void selectCard();
}